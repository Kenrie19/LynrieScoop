# Mosquitto configuration file

# Allow external connections
listener 1883 0.0.0.0
listener 9001 

# Allow WebSocket connections on port 9001
protocol websockets

# Allow anonymous connections (for testing, replace with auth if needed)
allow_anonymous true

